import React from 'react'
import { SectionSlider } from './styledComponents'

export const SectionSliderBg = React.forwardRef((props, ref) => {
  
  return (
    <SectionSlider name="home" >SectionSlider</SectionSlider>
  )
  
})
